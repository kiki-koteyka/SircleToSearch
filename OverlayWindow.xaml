<Window x:Class="SircleToSearch.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SircleToSearch"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        Background="Black"
        Cursor="Cross"
        WindowStartupLocation="Manual">
    <Grid x:Name="RootGrid"
          MouseLeftButtonDown="RootGrid_MouseLeftButtonDown"
          MouseMove="RootGrid_MouseMove"
          MouseLeftButtonUp="RootGrid_MouseLeftButtonUp"
          LostMouseCapture="RootGrid_LostMouseCapture">
        <Image x:Name="ScreenshotImage" Stretch="Fill"/>

        <!-- Dims everything except the selection rectangle, whose Clip carves a hole out of this. -->
        <Path x:Name="DimOverlay" Fill="Black" Opacity="0.55"/>

        <!-- A real Canvas: Grid ignores Canvas.Left/Top on its own children, so the
             selection border and its corner handles both live here to share one
             coordinate space that actually honors those attached properties. -->
        <Canvas x:Name="HandleLayer" IsHitTestVisible="False">
            <Rectangle x:Name="SelectionBorder" Visibility="Collapsed"
                       Stroke="White" StrokeThickness="2.5" Fill="Transparent"/>
            <Canvas x:Name="Handles"/>
        </Canvas>

        <Border VerticalAlignment="Top" HorizontalAlignment="Center"
                Margin="0,28,0,0" CornerRadius="18" Padding="18,10"
                Background="#CC202124">
            <TextBlock x:Name="HintText"
                       Foreground="White" FontSize="15" FontFamily="Segoe UI"/>
        </Border>
    </Grid>
</Window>
